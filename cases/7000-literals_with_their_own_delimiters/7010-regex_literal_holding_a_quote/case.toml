trap = """
a double quote inside a JavaScript regular expression, where it is part of the pattern and opens
no string. A JavaScript string cannot cross a line, so a counter fooled by it loses this one line
and recovers on the next"""
