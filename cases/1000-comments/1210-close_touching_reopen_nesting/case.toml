trap = """
a Haskell block comment closes and the next one opens with nothing between the two symbols. The
pair nests, so a counter tracking how deep it is must not read the reopener as two levels"""
