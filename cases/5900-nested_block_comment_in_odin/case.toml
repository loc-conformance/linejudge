trap = """
Odin block comments nest, so the first closer ends only the inner one and the text under it is
still inside the outer block"""

[answer.mezura.content]
counted = { lines = 6, code = 2, comments = 3, extra = 1 }

[answer.mezura.region]
counted = { lines = 6, code = 2, comments = 4, blanks = 0 }

[answer.scc.default]
counted = { lines = 6, code = 4, comments = 2, blanks = 0 }
note    = """
the block is ended at the inner closer, so the two lines after it are counted as code"""

[answer.tokei.default]
counted = { lines = 6, code = 4, comments = 2, blanks = 0 }
note    = """
the block is ended at the inner closer, so the two lines after it are counted as code"""
