trap = """
a line holding only a brace. A definition, not a mistake: neither code nor comment to a counter
that asks what a line says, code to one that asks whether the file would compile without it"""
