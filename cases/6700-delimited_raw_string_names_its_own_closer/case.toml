trap = """
a delimited raw string names its own terminator, so no declared symbol can close it"""

[answer.mezura.content]
counted = { lines = 4, code = 1, comments = 3, extra = 0 }
note    = """
the plain R"( is declared and this delimited form is not, so the block opener inside the string
opens a comment"""

[answer.mezura.region]
counted = { lines = 4, code = 1, comments = 3, blanks = 0 }
note    = """
the plain R"( is declared and this delimited form is not, so the block opener inside the string
opens a comment"""

[answer.scc.default]
counted = { lines = 4, code = 1, comments = 3, blanks = 0 }
note    = """
the delimited form is not recognised, so the block opener inside the string opens a comment"""

[answer.tokei.default]
counted = { lines = 4, code = 1, comments = 3, blanks = 0 }
note    = """
the delimited form is not recognised, so the block opener inside the string opens a comment"""
