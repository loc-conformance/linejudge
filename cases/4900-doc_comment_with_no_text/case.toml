trap = """
a doc comment line holding nothing but its marker, which is how a blank line is put between two
paragraphs of documentation. A bare marker says nothing, so it is extra to a counter that asks
what a line says"""
